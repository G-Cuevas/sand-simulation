{
  "name": "sand-simulation",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open --port 8080",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "serve": "vite --open --port 8080 --host"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.0.8"
  }
}
